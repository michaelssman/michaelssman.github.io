# 项目协作规范

## 文档整理与更新

- 整理和修改文档时，只保留经核实、当前有效且正确的知识点；优先依据官方文档，涉及版本和时效性的内容先核对最新资料。
- 发现过时、错误或已被替代的知识点，直接删除或替换，不保留旧方案、错误示例或历史纠错说明。
- 合并重复内容，删除冗余解释，只保留学习和使用必需的原理、步骤、示例及注意事项，保持简洁。
- 修改文档导致图片不再被仓库中的任何文档引用时，删除对应图片文件；图片所在目录已无任何文件时，同时删除该空目录。

## Java 初学者文档

- 新建或修改 `docs/Java/` 及其子目录中的文档时，以 Java 初学者为读者，不默认读者已掌握 Java、框架、后端或部署知识。
- 专业名词首次出现时，说明基础定义、用途以及在当前功能中的作用；缩写补充英文全称和中文解释。重复术语可集中到本文术语表，避免只用另一个未解释的术语来解释它。
- 功能教程先说明要实现的效果及前置准备，包括必要的软件与版本、依赖、项目结构、运行环境和已有配置；明确操作在本机、服务器还是容器中执行。
- 操作流程按实际执行顺序逐步编号，写清每一步的目的、执行目录、要创建或修改的文件路径及位置、具体代码或配置、执行命令或界面操作，不用“自行配置”“同理实现”等表述省略必要步骤。
- 代码与配置示例应相互衔接，形成完成该功能所需的最小可运行流程；解释关键参数，明确占位符需要替换成什么值、从哪里获取，避免把示例值误当作实际配置。
- 每个关键步骤给出检查方法和预期结果，最后提供完整功能的验证步骤；对容易遇到的错误说明原因和处理方法，让读者能够按照文档从准备、实现到验证逐步完成。
- 保持通俗、精简，删除重复和无关说明，但不以精简为由省略理解或实现当前功能所必需的基础定义和操作步骤。

## MkDocs 导航同步

- Git 提交前由 `.githooks/pre-commit` 自动运行 `python3 scripts/sync_mkdocs_nav.py`。
- 脚本以 Git 暂存区中的 `docs/` Markdown 文件为准，自动处理 `mkdocs.yml` 中 `nav` 的新增、删除和重命名，并自动暂存更新后的 `mkdocs.yml`。
- 同目录重命名保留原有分组和位置；新增文档或跨目录移动会按文件目录插入对应分组，缺少分组时使用目录名创建。
- 脚本保留已有中文标题和人工排序，只追加缺少的文档并移除失效路径。
- `docs/.hidden/` 及其他名称以 `.` 开头的目录用于保存不公开的文档，目录中的 Markdown 文件不得加入 `mkdocs.yml` 的 `nav`。
- 如果 `mkdocs.yml` 存在未暂存修改，脚本必须停止，避免覆盖用户修改；先处理或暂存这些修改后再提交。
- 不要使用 `--no-verify` 绕过同步。
- 首次克隆仓库后运行 `git config core.hooksPath .githooks`，启用仓库内维护的提交前校验。
