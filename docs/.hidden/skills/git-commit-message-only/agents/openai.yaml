interface:
  display_name: "Git Commit Message Only"
  short_description: "Generate scoped commit messages from diffs without code review"
  default_prompt: "Use $git-commit-message-only to generate `type(scope): summary` commit messages from the current git diff without reviewing code, without brackets, and with body details when one summary line is not enough."
