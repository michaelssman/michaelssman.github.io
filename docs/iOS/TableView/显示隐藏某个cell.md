# 数据驱动页面

根据数据去显示UI，决定是否显示。

# 总结：

界面是界面，数据源是数据源。

界面的数据是数据源中取的。

使用数据的时候是从数据源中获取，不要从界面上取数据。



如果没有数据，需要把某些行不显示。

做法

cell个数是所有cell都显示的情况，满的情况。

cell的返回cell的代理和section个数row个数代理都是所有cell都显示完全的情况。

cell 的height中判断哪些cell不显示。只需把height返回0。

